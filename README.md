# Google Storage reader

## Instalation

- Run `composer install`
- Run `cp config/config.json.dist config/config.json`
- Fill config/config.json with needed credentials
- Run `bin/caller` to get help about command
